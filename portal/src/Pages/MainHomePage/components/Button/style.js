import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
  Navbutton: {
    backgroundImage: "linear-gradient(to right, #649bff, #0070fa, #649bff)",
    padding: "0.5rem 2rem",
    textAlign: "Center",
    fontSize: "14px",
    color: "#fff",
    border: "none",
    boxShadow: "none",
    borderRadius: "5px"
  }
}));
export default useStyles;
