import { getSafeInsance } from "Helper";

export const getSafeInsightsInfo = getSafeInsance(["data", "info"]);
export const getSafeInsightsCharts = getSafeInsance(["data", "charts"]);
export const getSafeInsightsTable = getSafeInsance(["data", "insights"]);
